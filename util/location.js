const getCoordsForAddress = (address) =>{

    // we did hard code lt & lng but we can also did with google geo api
     return {
         lat:40.66666,
         lng:-72.838939
     };
}

module.exports = getCoordsForAddress;